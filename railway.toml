# GhostCatcher compact read-only research export
[build]
builder = "DOCKERFILE"
dockerfilePath = "ingest/Dockerfile.ghostcatcher-export"

[deploy]
startCommand = "python /app/ghostcatcher_compact_export.py"
restartPolicyType = "NEVER"
