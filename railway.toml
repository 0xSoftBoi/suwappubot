# GhostCatcher read-only research export service
[build]
builder = "DOCKERFILE"
dockerfilePath = "ingest/Dockerfile.ghostcatcher-export"

[deploy]
startCommand = "python /app/ghostcatcher_snapshot_server.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 2
