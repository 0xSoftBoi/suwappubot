{
  "name": "@suwappu/sdk",
  "version": "0.2.0",
  "description": "Cross-chain DEX SDK — swaps, perps, predictions, and lending for AI agents",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "bin": {
    "suwappu": "src/cli.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "keywords": ["suwappu", "dex", "cross-chain", "agent", "sdk", "swap"],
  "dependencies": {
    "@suwappu/openclaw": "^0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
