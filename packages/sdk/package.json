{
  "name": "@suwappu/sdk",
  "version": "0.3.0",
  "description": "Cross-chain DEX SDK — swaps, perps, predictions, and lending for AI agents",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "bin": {
    "suwappu": "src/cli.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "keywords": ["suwappu", "dex", "cross-chain", "agent", "sdk", "swap"],
  "dependencies": {
    "@suwappu/openclaw": "^0.2.0",
    "commander": "^12.0.0",
    "chalk": "^5.3.0",
    "cli-table3": "^0.6.5",
    "ora": "^8.0.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.10",
    "typescript": "^5.6.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
