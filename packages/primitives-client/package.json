{
  "name": "@suwappu/primitives-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Typed viem client for the immutable Suwappu on-chain primitives (TimeCurve, AmortizingVault, MutualCredit).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "test:node": "node --import tsx --test \"test/**/*.test.ts\""
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "tsx": "^4.23.12",
    "typescript": "^5.6.0"
  }
}
