# Public production API origin. This is intentionally non-secret and prevents
# Vite from silently falling back to same-origin requests on the static terminal
# host when Railway environment configuration is incomplete.
VITE_API_URL=https://api.suwappu.bot
