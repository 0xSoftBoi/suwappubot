# Dependencies
node_modules/

# Build output
dist/

# Drizzle migrations ARE committed (drizzle/ was previously ignored, which meant
# GitHub-source Railway builds shipped no migration files → drizzle-kit migrate had
# nothing to apply and the dangerous push --force fallback ran. Migrations are the
# reviewed source of truth and must be in the repo. Do NOT re-ignore drizzle/.

# Environment
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Bun lockfile is committed for reproducible builds
