[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Disable web3 pytest plugin to avoid import conflicts
addopts = -p no:web3 -p no:ethereum

# Markers
markers =
    asyncio: mark test as async
    slow: marks tests as slow

