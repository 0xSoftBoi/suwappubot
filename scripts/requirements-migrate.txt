# Extra dependency for the one-time KMS re-wrap migration (scripts/rewrap_kms_to_local.py).
# Run in an environment that still has AWS access, ON TOP of the main requirements.txt:
#   pip install -r requirements.txt -r scripts/requirements-migrate.txt
# boto3 is kept out of the runtime image (see requirements.txt) to avoid a
# botocore<->urllib3 conflict and to slim the image once we're on KMS_PROVIDER=local.
boto3>=1.43.68
