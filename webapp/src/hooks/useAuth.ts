/**
 * Main auth hook - re-exports from AuthContext
 *
 * This hook provides access to the unified auth state and actions.
 * Use this as the primary way to interact with authentication.
 */

export { useAuth, formatAddress } from '../contexts/AuthContext'
